export enum TaskPriority {
  Default = 1,
  Low = 2,
  Medium = 3,
  High = 4,
  Highest = 5,
}

export enum TaskStatus {
  PENDING = 'Pending',
  IN_PROGRESS = 'In progress',
  COMPLETED = 'Completed',
  CANCELED = 'Canceled',
}
